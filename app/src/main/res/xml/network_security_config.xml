<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow user certificates (like Charles Proxy) in debug builds -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user-installed certificates (Charles, Fiddler, etc.) -->
            <certificates src="user" />
            <!-- Also trust system certificates -->
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
    
    <!-- Base configuration for all connections -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <!-- Trust system certificates by default -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Specific domain configurations can be added here if needed -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">api.themoviedb.org</domain>
        <domain includeSubdomains="true">image.tmdb.org</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
